services:
  gjallar:
    build: .
    image: gjallar:latest
    restart: unless-stopped

    ports:
      - "127.0.0.1:8000:8000"

    env_file:
      - backend/.env

    environment:
      SIGNAL_DATABASE_PATH: /data/signal.db
      SIGNAL_FRONTEND_DIR: /app/frontend_dist
      SIGNAL_DEBUG: "false"
      # Secrets written by the setup wizard live on the volume, not in the
      # container filesystem — otherwise rebuilding the image loses the admin
      # token and the VAPID keys, and every device has to re-subscribe.
      SIGNAL_ENV_FILE: /data/.env

    volumes:
      - gjallar-data:/data

    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  gjallar-data: